java => pojo  = plain old java obejcts
.net => poco = plain old c# objects

-entity class has feilds attributes constructors

DAO = data access object
DAL = data access layer

properties in .net called smart field
-deferred property {get{} protected set{}};
-Auto_Implemented eg:public int Age { get; set; }
-CLR Property eg: get_Age() set_Age(int value)

getter/setter in java properties in c#


ref-> when we pass by ref  out->when we want to return multiple things from a function
if function/method is non-static then call it creating object of the class in which that function is been created

enum -> 

//2D array
int[ , ] arrNum = new int[38,4];
//jagged array->manage resource efficiently as in every row we can add new dimention
int[][] arrNum = new int[][]
arr num[0] = nedw int[4]
arrNum[1] = new int[2]

abstract classes -> can have multiple childs not objects (like virtual classes)
    - atleast one method should be abstract which will be overriden
sealed classes -> multiple objects not the childs
interface - multiple inheritance
concrete class - classes apart from abstract and sealed 
    - we can make virtual method which can be overriden 


Constructor chaining : process of invoking one constructor from another using this() or base() to reuse initialization code.
-Constructor Chaining within the Same Class (this()) : One constructor calls another constructor of the same class
-Constructor Chaining with Inheritance (base()) : Used when a derived class constructor calls base class constructor


Partial types : 
