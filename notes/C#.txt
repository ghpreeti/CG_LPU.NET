concepts

COM->DCOM->DLL->Remoting->MSMQ->WebServices->WCF->WebAPI
                                  |                 |
data transfer                   XML               JSON
NIC - network card comes with limitation as per device

type(Primitive)
-no heap allocation,less garbage collection
-more efficient memeory
-less reference indirection
-unified type System(predefined types)

data type conversion
-explicit (high->low)
-implicit (low->high)

enum- named integer constant(user defined)

-stack holds refrences
-heap stores objects

Exception
-> ApplicationException

Delegates => function pointer pointing towards named function
-function inter and base for event handling
-when delegate return type and method return type mismatch like child class and delegate is of -parent type = Covariance
when method looking for parameter as child and we are passing parent type in delegate : Contravariance

Serialization
-Binary(fastest): for same domain
-XML: for different domain (sensitive data also get exposed)
-Soap(simple object access protocol):for cross domain (sensitive remain sensitive)
-JSON : for modern Hardware

Serialize : object to bytes
Deserialize : bytes to object

- sensitive date (non-serialize) rest can be serialized = opt-out model


DLR(dynamic language )
var
-Compile-time type inference
-Type is decided at compile time
-Still strongly typed

✔ Use var when:
Type is obvious
LINQ queries
Anonymous types


Dynamic
-Run-time type resolution
-Type checking happens at runtime
-Not strongly typed (compiler skips checks)

✔ Use dynamic when:
Working with COM objects
Reflection
JSON / loosely typed data
Interoperability (JavaScript, Python)




