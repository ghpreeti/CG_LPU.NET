applications
-In-memeory: list,dict,sorted dict etc.
-Files: xml->XPath
-Database: ADO.net
           -Connected : live data(less scalable) one user+one active connections,data called directly from database
           -Disconnect: data copied into memeory then closes immediately,happens offline,open for brief time only

=>LINQ : language integrated query 
          
String/StringBuilder
=>String(immutable)
    -Compare
    -str.Concat(string)
    -Contain
    -IndexOf(Char)
    -Insert(char,Int)

=> StringBuilder(mutable)

=>Action : delegate
    -Action(void type)
    -no. of arg in method = input parameter<> in Action delegate to match signature

    -Action<int> square = delegate(int num){ //anonymous delegate
        Console.WriteLine(num*num);
        } square(5);

    -Action<string> lbdDel = (message) => Console.WriteLine($"{message}") // action anonymous with lamda method
     lbdDel("Hello");   

=>Func : delegate
    -return type needed(like unicast)
    -Func<int,int,int> powDel = Math.Pow; // 2 input and one return value
    -Func<String,bool> strDel = String.IsNullEmpty; //string:input,bool:output

=>Predicate : delegate
    -bool type
    -Predicate<string> str = string.IsNullOrEmpty; //only returns true/false

=>Reflections
             
